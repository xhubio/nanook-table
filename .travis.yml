language: node_js
node_js:
- 12.14
sudo: false
branches:
  only:
  - master
  - /^greenkeeper-.*$/
  - /^greenkeeper/.*$/
cache:
  directories:
  - node_modules
notifications:
  email:
  - torstenlink@gmx.de
install:
- yarn install
script:
- yarn test
after_success:
- npx semantic-release
